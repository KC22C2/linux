  in-tree:
    CONFIG_ARCH_APPLE=y
    CONFIG_PCIE_APPLE_MSI_DOORBELL_ADDR=0xfffff000
    CONFIG_PCIE_APPLE=y
    CONFIG_NVME_APPLE=y
    CONFIG_I2C_APPLE=y
    CONFIG_SPI_APPLE=y
    CONFIG_PINCTRL_APPLE_GPIO=y
    CONFIG_APPLE_WATCHDOG=y
    CONFIG_SPI_HID_APPLE_OF=y
    CONFIG_SPI_HID_APPLE_CORE=y
    CONFIG_APPLE_MAILBOX=y
    CONFIG_APPLE_DART=y
    CONFIG_APPLE_PMGR_PWRSTATE=y
    CONFIG_APPLE_RTKIT=y
    CONFIG_APPLE_SART=y
    CONFIG_APPLE_AIC=y
    CONFIG_USB_DWC3=y
    CONFIG_BRCMFMAC=y
    CONFIG_BRCMFMAC_PCIE=y
    CONFIG_DRM_SIMPLEDRM=y
    CONFIG_ARM_APPLE_SOC_CPUFREQ
    CONFIG_SPMI_APPLE
    CONFIG_GPIO_MACSMC
    CONFIG_POWER_RESET_MACSMC
    CONFIG_CHARGER_MACSMC
    CONFIG_RTC_DRV_MACSMC
    CONFIG_APPLE_PLATFORMS
    CONFIG_APPLE_SMC
    CONFIG_APPLE_SMC_RTKIT
    CONFIG_INPUT_MACSMC_HID
    CONFIG_APPLE_M1_CPU_PMU
  see config file in tree (same commit at this one)

  following will be required for audio
    CONFIG_SND_SOC_APPLE_MCA=y
    CONFIG_SND_SOC_TAS2770=m
    CONFIG_SND_SIMPLE_CARD=m
    CONFIG_APPLE_ADMAC=y
    CONFIG_COMMON_CLK_APPLE_NCO=y
    CONFIG_SND_SIMPLE_CARD=y
    CONFIG_SND_SOC_CS42L42=y

  following may be harmful
    CONFIG_FW_LOADER_USER_HELPER_FALLBACK=n
    CONFIG_FW_LOADER_USER_HELPER=n

  not truly M1-related, but just so it's documented somewhere
    CONFIG_FONT_TER16x32=y  # so the console is big enough to be read
    CONFIG_VFAT_FS=y        # to work on the ESP partition (see Debian config for options)
    CONFIG_EXFAT_FS=y       # just in case
    CONFIG_USB_RTL8152=y    # for the USB-ethernet adapter
    CONFIG_NLS_CODEPAGE_437 # might be required to mount /boot/efi
    CONFIG_EFI=y            # required for boot from SSD, through u-boot/grub


Linux kernel
============

There are several guides for kernel developers and users. These guides can
be rendered in a number of formats, like HTML and PDF. Please read
Documentation/admin-guide/README.rst first.

In order to build the documentation, use ``make htmldocs`` or
``make pdfdocs``.  The formatted documentation can also be read online at:

    https://www.kernel.org/doc/html/latest/

There are various text files in the Documentation/ subdirectory,
several of them using the Restructured Text markup notation.

Please read the Documentation/process/changes.rst file, as it contains the
requirements for building and running the kernel, and information about
the problems which may result by upgrading your kernel.
